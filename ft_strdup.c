#include "libft.h"

char	*ft_strdup(const char *s)
{
	const size_t	size = ft_strlen(s) + 1;
	char			*strdup;

	str = (char *)malloc(size);
	if (dup != NULL)
		ft_strlcpy(dup, s, size);
	return (dup);
}
